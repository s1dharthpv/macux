# MacUX Python dependencies
# Install with: pip3 install -r requirements.txt

# ─── Core ────────────────────────────────────────────────────────────────────
PyGObject>=3.48.0          # GTK4 / GLib / Gio / Adwaita Python bindings
dasbus>=1.6.0              # Modern, type-safe Python DBus library

# ─── Configuration ───────────────────────────────────────────────────────────
# tomllib is in stdlib since Python 3.11 — no install needed
tomli_w>=1.0.0             # TOML serialization (write support)
watchdog>=4.0.0            # Filesystem change monitoring for config/index

# ─── Search engine ───────────────────────────────────────────────────────────
Whoosh>=2.7.4              # Pure-Python full-text search
# inotify_simple>=1.3.1    # Linux inotify watcher (optional, faster than watchdog)

# ─── System integration ──────────────────────────────────────────────────────
pulsectl>=23.5.0           # PulseAudio/PipeWire volume control
# NetworkManager:  via GObject introspection (libnm) — system package
# BlueZ:           via dbus directly — no extra package
# UPower:          via dbus directly — no extra package

# ─── File management ─────────────────────────────────────────────────────────
Pillow>=10.0.0             # Image processing — thumbnails, icon scaling
pyxattr>=0.8.0             # Extended filesystem attributes (file tags)

# ─── Utilities ───────────────────────────────────────────────────────────────
appdirs>=1.4.4             # XDG-compliant data/cache/config directories

# ─── Development / testing ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
black>=24.0.0
ruff>=0.4.0
mypy>=1.10.0
